<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8">
  </head>
  <body>
    <div>
      @yield('contenido1')
      @yield('contenido2')
      @yield('contenido3')
      @yield('contenido4')
    </div>
  </body>
</html>